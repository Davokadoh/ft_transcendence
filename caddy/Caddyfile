https://{$DOMAIN} {
    handle_path /static/* {
        root * /static
        file_server
    }

    handle_path /media/* {
        root * /media
        file_server
    }

    handle {
        reverse_proxy django:8000
    }
    tls /ssl/cert.crt /ssl/privkey.key
}